## 1. Load-time validation (rewrite the reference switch)

- [x] 1.1 Add `regexp` import + package-level `const envNamePattern = "^[A-Za-z_][A-Za-z0-9_]*$"`
      and `var envNameRe = regexp.MustCompile(envNamePattern)`.
- [x] 1.2 Reject `env` without `path` → `HOMEMANAGER_SECRET_ENV_REQUIRES_PATH` (ordered before the
      name check so the loader surfaces it first).
- [x] 1.3 Reject an `env` name not matching the pattern → `HOMEMANAGER_SECRET_INVALID_ENV_NAME`
      (also blocks Nix-attr injection before any emission).
- [x] 1.4 Keep neither-path-nor-env → `HOMEMANAGER_SECRET_MISSING_REF` (wording widened to mention
      the optional `env`). Remove `HOMEMANAGER_SECRET_ENV_UNSUPPORTED`.
- [x] 1.5 Net: path-only ✓, env+path ✓ (NEW), env-only ✗, neither ✗; backend/name/duplicate checks
      unchanged.

## 2. Compiler (env path-reference; no-embed preserved)

- [x] 2.1 `compileSecretsModule` adds `case s.Env != "":` FIRST → `home.sessionVariables.<env> =
      "<path>";` (env emitted as a bare attr, safe via the load-time regex). Path-only `home.file`
      case unchanged, sorted determinism preserved, never `os.ReadFile`.
- [x] 2.2 STRUCTURAL: a sentinel at an env secret's `path` is absent from every generated byte while
      the sessionVariable reference is present.

## 3. Capture (reference only — no production change)

- [x] 3.1 Prove an env+path secret round-trips through capture (`Env`/`Path` carried verbatim) with
      NO code change, by extending the capture round-trip test.

## 4. Doc-comments (truthful env path-reference semantics)

- [x] 4.1 `HomeManagerSecret` (`types.go`), `compileSecretsModule` (`home_secrets.go`),
      `validateHomeManagerSecrets` (`validator.go`) — drop the "PATH-ONLY/env deferred" language.

## 5. Verification

- [x] 5.1 `go test ./...`, `go vet ./...`, `GOOS=windows go build ./...` all green.
- [x] 5.2 `openspec validate --all --strict --no-interactive` passes.
- [ ] 5.3 Real-nix smoke (best-effort): an env+path secret manifest pointing at a temp sentinel →
      apply → `home.sessionVariables.<env>` references the path, sentinel ABSENT from the generated
      tree and `/nix/store`. (Parent re-runs a secrets round-trip; note any environment-only
      failure.)
