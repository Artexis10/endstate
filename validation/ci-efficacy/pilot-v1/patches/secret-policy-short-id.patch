diff --git a/go-engine/internal/bundle/capture_bundle.go b/go-engine/internal/bundle/capture_bundle.go
index dfb6eee214d18dcb1a973ae085981a83840c51eb..d94a36bf117cfa512c00bf9fce0ae331240f3f3a 100644
--- a/go-engine/internal/bundle/capture_bundle.go
+++ b/go-engine/internal/bundle/capture_bundle.go
@@ -736,7 +736,11 @@ func collectLegacyCaptureLanes(candidates []*modules.Module, stagingRoot string,
 		_ = os.RemoveAll(workRoot)
 
 		legacy.included = append(legacy.included, shortID)
-		legacy.moduleIDs = append(legacy.moduleIDs, mod.ID)
+		if mod.Secrets != nil && len(mod.Secrets.Files) > 0 {
+			legacy.moduleIDs = append(legacy.moduleIDs, shortID)
+		} else {
+			legacy.moduleIDs = append(legacy.moduleIDs, mod.ID)
+		}
 		legacy.capturedModules = append(legacy.capturedModules, mod)
 		legacy.modules = append(legacy.modules, LegacyModuleCaptureResult{
 			ModuleID:        mod.ID,
